{
  "name": "developer-card-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend run test",
    "check:compose": "docker compose -f docker-compose.yml -f docker-compose.dev.yml config --quiet"
  }
}
